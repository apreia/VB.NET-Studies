Imports System

Module Program
    Sub Main()
        Dim N As Integer = 10
        Console.WriteLine("A diferença entre o quadrado da soma e a soma dos quadrados para N = " & N & " é: " & DifferenceOfSquares.CalculateDifferenceOfSquares(N))
    End Sub
End Module